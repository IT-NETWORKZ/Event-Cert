# TODO - Subscription hero responsiveness fix

- [x] Update `src/components/admin/Subscription/Subscriptionplan.css` to make `.subscription-hero` responsive by removing the fixed `width: 900px` and using `width: 100%` (with `max-width`).
- [x] Adjust `.subscription-hero` margin in the `@media (max-width: 640px)` breakpoint.
- [ ] Re-run/start dev server and verify hero alignment on mobile (<640px) and tablet (<=900px).

